<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="social"
            type="com.peerpongsam.profile.model.SocialNetwork" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical">

        <androidx.appcompat.widget.AppCompatTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:drawableStart="@drawable/ic_github"
            android:drawablePadding="8dp"
            android:drawableTint="?colorAccent"
            android:gravity="center"
            android:padding="8dp"
            android:text="@{`https://github.com/` + social.github}"
            android:textAppearance="?textAppearanceCaption"
            tools:ignore="UnusedAttribute"
            tools:text="\@peerapongsam" />

        <androidx.appcompat.widget.AppCompatTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:drawableStart="@drawable/ic_facebook"
            android:drawablePadding="8dp"
            android:drawableTint="?colorAccent"
            android:gravity="center"
            android:padding="8dp"
            android:text="@{`https://fb.me/` +social.facebook}"
            android:textAppearance="?textAppearanceCaption"
            tools:ignore="UnusedAttribute"
            tools:text="\@peerapongsam" />

        <androidx.appcompat.widget.AppCompatTextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:drawableStart="@drawable/ic_linked_in"
            android:drawablePadding="8dp"
            android:drawableTint="?colorAccent"
            android:gravity="center"
            android:padding="8dp"
            android:text="@{`https://www.linkedin.com/in/` + social.linkedIn}"
            android:textAppearance="?textAppearanceCaption"
            tools:ignore="UnusedAttribute"
            tools:text="\@peerapongsam" />

    </LinearLayout>
</layout>